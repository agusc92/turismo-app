// Archivo principal de configuración para la API
// Cambia esta IP por la IP local de tu computadora cuando pruebes en tu celular (por ej: 192.168.0.45)
// Si usas emulador Android en la misma PC, puede que necesites usar 10.0.2.2

export const IP_ADDRESS = "192.168.0.45";
export const API_URL = `http://${IP_ADDRESS}:8000/api`;
